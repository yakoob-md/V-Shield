You are an expert full-stack developer specializing in debugging UI + backend integrations.

I have a chatbot application where:

Each generated answer has a "Narration" (Text-to-Speech) button
When I click the narration button → nothing happens
The expected behavior is:
It should read out the answer aloud
If the answer is in English → read in natural English voice
If the answer is in Hinglish → read it in Hinglish pronunciation (not robotic English)
🎯 YOUR TASK
1. ROOT CAUSE ANALYSIS

Carefully analyze and identify ALL possible reasons why narration is not working:

Is the click event properly attached?
Is the function being triggered?
Is the TTS API being called?
Any console errors?
Is browser speech API (speechSynthesis) used incorrectly?
Is there an async issue or missing permissions?
Is state/context not passing the answer text correctly?
Is the narration button mapped to the correct message?
2. FIX IMPLEMENTATION (MANDATORY)

Provide complete working code fixes, including:

✅ Frontend Fix
Proper event listener for narration button
Extracting correct message text
Handling multiple messages (important!)
Prevent overlapping speech (stop previous before new)
Show loading / speaking state (optional but preferred)
✅ TTS Implementation

Use one of:

Browser API: window.speechSynthesis
OR
Better: suggest upgrade to Google TTS / ElevenLabs (optional)

Must include:

speechSynthesis.cancel();
const utterance = new SpeechSynthesisUtterance(text);
speechSynthesis.speak(utterance);
3. HINGLISH SUPPORT (IMPORTANT)

Implement logic:

Detect Hinglish (mixed Hindi-English)
Use appropriate voice:
Prefer hi-IN or Indian English voice
Ensure pronunciation sounds natural

Example:

"Tum kya kar rahe ho?" → should sound Hindi
"This is important samjho" → mixed Hinglish tone
4. HANDLE EDGE CASES
Multiple narration clicks
Switching between messages
Long responses (chunk if needed)
Stop button support (if possible)
5. CODE STRUCTURE (VERY IMPORTANT)

Explain clearly:

Which file to edit (e.g., index.html, main.js, chat.js)
What EXACT code to replace
What to add
Label sections clearly:
// OLD CODE
// NEW CODE
6. DEBUGGING SUPPORT

Add console logs:

console.log("Narration clicked", text);

Also mention:

How to test in browser
What to check in console
⚡ EXPECTED OUTPUT
Clear diagnosis
Fully working code
Hinglish + English narration support
Clean UI behavior (no bugs)
⚠️ IMPORTANT

Do NOT give generic explanation.
Give direct working fix with code and exact integration steps